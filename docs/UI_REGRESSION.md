# 界面回归记录

## 2026-09-13 至 2026-09-14：移动端、键盘与数据刷新

环境：Chromium 内嵌浏览器；GitHub Pages 已发布站点与本地静态构建。以下为实际交互检查，不代表所有浏览器或屏幕阅读器的完整兼容性认证。

| 场景                         | 结果                                                                                              |
| ---------------------------- | ------------------------------------------------------------------------------------------------- |
| 390 × 844 视口的五个导航标签 | 修复前文档宽 451px，内容视口 375px；修复后两者均为 375px，标签在自身区域滚动                      |
| 320 × 844 窄屏               | 数据页、EI 筛选与期刊卡片无整页横向溢出，文档与内容视口均为 305px                                 |
| 1280 × 900 桌面              | 期刊卡片、分区与索引标签布局正常，文档与内容视口均为 1265px                                       |
| 键盘导航                     | 在期刊标签按 End 移动到最后一个标签，Enter 成功打开“数据与共建”                                   |
| 键盘筛选                     | Enter 打开索引选项，从 ESCI 向下选择 EI，确认显示 23 本期刊                                       |
| 跳转到内容                   | Enter 激活跳转链接后，焦点落在 main#content，当前“激光”搜索与 ESCI 筛选保留                       |
| 分享条件恢复                 | 通过含 index=ESCI 的期刊 URL 打开，正确恢复 ESCI 筛选                                             |
| 加载新版                     | 线上从旧版本检查更新并加载到 21 届会议版本；水合完成后“激光”搜索、ESCI 筛选及《中国激光》关注保留 |

测试新增的关注已取消。视口宽度扣除了桌面浏览器滚动条，故内容视口比设置值少 15px。

修复：导航列表限定最大宽度并允许内部横向滚动；主内容增加程序化焦点落点；侧栏提示改为“数据核验日期见各条目”，避免固定初始快照日期被误解为最新核验时间。

验证：类型检查、lint 和静态构建通过。构建第一次遇到 Windows Node 句柄关闭断言，重新执行完整构建成功。后续新增筛选项、导航标签或刷新逻辑时，复用这些交互场景。
